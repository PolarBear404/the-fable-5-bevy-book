# 全书大纲（待起草）

本文件在大纲阶段产出，经用户审定后成为全书章节范围的唯一依据（见 CLAUDE.md）。

起草要求：

- 基于 `vendor/bevy/` 的 crate/模块清单与 `vendor/bevy/examples/` 盘点 Bevy 0.18.1 的全部功能领域，
  确保"全覆盖"有源码依据，而不是凭印象罗列
- 章节顺序循序渐进，前章不依赖后章概念；像 The Rust Book 一样可以从头读到尾
- 每章列出：学习目标、覆盖的 API/模块、配套示例项目的形态
- 官方核心功能为主体；生态第三方 crate（物理引擎等）单独标注为附录候选，由用户决定取舍
