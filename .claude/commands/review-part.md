---
description: 交叉审阅已完成的章节（技术准确性、代码一致性、体例一致性），只报告不修改
argument-hint: <章节范围，如 1-5>
---

审阅第 $ARGUMENTS 章。用并行子代理执行三类独立检查，汇总成一份报告。本命令只产出报告，
未经用户确认不得修改任何文件。

1. **技术准确性**：正文中对 Bevy 行为/API 的每一处断言，对照 vendor/bevy/ 源码核实；
   列出错误、过时说法、以偏概全的表述。
2. **代码一致性**：相关 crate 全部 `cargo check`；book/src/ 的每个 {{#include}} 都能解析；
   正文描述的运行结果与代码实际行为一致；Listing 编号与文件一一对应。
3. **体例一致性**：术语是否统一（英文术语用法）、标题层级、文件命名、链接有效性，
   以及与 CLAUDE.md 行文规范的偏差。

报告按 章节 → 严重度（错误 / 过时 / 风格）分组，每条给出文件位置和修改建议。
