# 双缓冲与清理时机

把 Listing 7-1 的链子故意反过来，让读者跑在写者前面。系统本身不用动（只是这回球不再每帧报告位置，改成撞墙时喊一声，方便对时）：

```rust
{{#include ../../code/ch07-messages/examples/listing-07-03.rs:main}}
```

<span class="caption">Listing 7-3：读者排在写者之前——音效慢一帧，但一条不丢</span>

```console
cargo run -p ch07-messages --example listing-07-03
```

```text
—— 第 1 帧 ——
—— 第 2 帧 ——
—— 第 3 帧 ——
球：撞墙！
—— 第 4 帧 ——
音效：砰！
—— 第 5 帧 ——
—— 第 6 帧 ——
球：撞墙！
—— 第 7 帧 ——
音效：砰！
```

第 3 帧撞的墙，第 4 帧才响。不难理解：第 3 帧轮到 `play_sound` 时消息还没写下，它扑了个空；等 `move_ball` 写完，本帧已经没有它的戏份。真正值得追问的是第 4 帧——**为什么消息还在？**谁把它留到了下一帧，又是谁保证它不会在第 5 帧再响一次？

## 缓冲的内部构造

答案在 `Messages<M>` 的结构里。它内部是**两个仓位**：一个收本帧写入的消息，一个存放上一帧的存货。`add_message` 除了创建这个资源，还注册了一个清理系统，在每帧最早的 `First` 调度（第 6 章的"引擎备料区"）里跑一次，做一套固定动作：**倒掉旧仓，把新仓整体挪作旧仓，腾出空仓收本帧。**

照这个节奏推演一条消息的一生：第 N 帧写入新仓；第 N+1 帧开头被挪进旧仓，整帧仍然可读；第 N+2 帧开头随旧仓一起倒掉。**每条消息恰好可见两帧**——这就是双缓冲（double buffering）。`read()` 的可见范围由此完全确定：两个仓位里、自己的游标还没扫过的全部消息。

Listing 7-3 的输出逐帧都能对上这套规则：第 3 帧的消息在第 4 帧仍可读，于是音效补上；响过之后游标已越过它，第 5 帧就算它还没被清掉也不会重读——双缓冲管寿命，游标管去重，两件事互不越界。

于是读者的勤快程度直接决定收成：

- **每帧都读**：一条不丢。这是消息系统的设计前提。
- **每两帧读一次**：贴着保质期飞行，能否全收齐取决于你和写者的相对顺序，不要依赖。
- **更慢**：必然丢消息——而且是**静默丢弃**，没有警告，没有错误。

口说无凭，让球加速到每帧都撞墙，再让记分员打瞌睡、每三帧才醒来读一次。消息带上序号，丢没丢一目了然：

```rust
{{#include ../../code/ch07-messages/examples/listing-07-04.rs:main}}
```

```rust
{{#include ../../code/ch07-messages/examples/listing-07-04.rs:systems}}
```

<span class="caption">Listing 7-4：错过即丢——打瞌睡的记分员每三帧才醒一次</span>

```console
cargo run -p ch07-messages --example listing-07-04
```

```text
—— 第 1 帧 ——
球：第 1 次撞墙
—— 第 2 帧 ——
球：第 2 次撞墙
—— 第 3 帧 ——
球：第 3 次撞墙
记分员醒来：听到第 [2, 3] 次
—— 第 4 帧 ——
球：第 4 次撞墙
—— 第 5 帧 ——
球：第 5 次撞墙
—— 第 6 帧 ——
球：第 6 次撞墙
记分员醒来：听到第 [5, 6] 次
—— 第 7 帧 ——
球：第 7 次撞墙
—— 第 8 帧 ——
球：第 8 次撞墙
—— 第 9 帧 ——
球：第 9 次撞墙
记分员醒来：听到第 [8, 9] 次
```

写了 9 条，读到 6 条。第 1、4、7 次碰撞无声无息地蒸发了——每次醒来只剩"昨天和今天"的消息，更早的已随旧仓倒掉。这不是 bug 而是设计：缓冲不清理就会无限膨胀，Bevy 选择了固定两帧的保质期，把"勤快地读"定为读者的本分。顺带可以验证上一节的论断：跳过的帧里游标原地不动，醒来时把两个仓位里没见过的一次读完，所以每次听到的是两条而不是一条。

## FixedUpdate 的特殊照顾

两帧保质期有一个明显的受害者：第 6 章说过，`FixedUpdate` 按自己的时钟走，高帧率下可能连续好几帧一轮都不跑。要是清理照常进行，放在 `FixedUpdate` 里的读者岂不是注定丢消息？

Bevy 替你想过了。带 `TimePlugin` 的 App（`MinimalPlugins` 和 `DefaultPlugins` 都含）会协调清理节奏：每轮固定更新结束时发一个信号，`First` 里的清理系统**只在收到信号后才真正动手**。也就是说，`FixedUpdate` 一轮没跑的帧，消息一条也不清；规则从"可见两帧"放宽成"至少活到 `FixedUpdate` 看过一眼"。放在 `Update` 还是 `FixedUpdate` 里的读者因此都安全，前提不变：每次轮到自己时把消息读掉。固定时间步的全貌在第 18 章。

> 本章的实验全部用裸 `App::new()`，没有 `TimePlugin`，清理系统每帧照跑——所以 Listing 7-4 才能呈现教科书式的两帧保质期。换成 `MinimalPlugins` 再跑一遍，丢消息的位置就会随固定时钟漂移，不再可复现。

把本节收拢成一句话：**消息是易腐品**。它是通知，不是存档——读到之后需要留底的信息，写进 Resource 或 Component；试图把消息当长期状态用，迟早在保质期上栽跟头。

寿命规则清楚了，下一节清点工具箱里剩下的零件。
