[package]
name = "ch08-events-observers"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

[dependencies]
# debug feature 让"亲眼看报错"示例的 panic 信息带上真实的系统名与类型名
bevy = { workspace = true, features = ["debug"] }
